/**
 * 1 create a new projects in console.google.com 
 * 2 Then install npm firebase in my projects
 * 3 Then create auth and import auth from firebase then export default auth;
 * 3 go to authentication and set email and password auth;
 * 4 Create a input field where i will create sing up and login from 
 * 5 then set some state for different input field 
 * 6 then add some handler and set in input field.
 **/

/**
 * firebase login command firebase login
 * firebase init command firebase init
 * firebase diploy command  firebase deploy

**/